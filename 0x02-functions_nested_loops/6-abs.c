#include "main.h"
/**
 * _abs - computes the absolute value
 *
 * @n: the character checker
 *
 * Return: Absolute value
 */
int_abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
